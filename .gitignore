node_modules/
# Jig runtime data is per-target-repo, never committed in this source repo:
.jig/
.sdlc/
# Scratch used by tests:
.tmp-test/
# Design docs (specs / plans) — kept local, not published:
docs/
