{
  "name": "jig",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Jig — a portable, gated software development lifecycle for AI coding agents, shipped as a Claude Code skill.",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test"
  }
}
